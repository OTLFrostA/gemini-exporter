# ⚠️ 待用户真机跑——本目录所有结论在真机跑完之前一律视为未验证

> **本目录（`tests/manual/local-font-access/`）是手动真机测试包。**
> Local Font Access 的真实授权 UX（permission prompt 的允许 / 拒绝 / 关闭、系统缺 CJK 字体、导出中途字体切换）
> 在 headless 环境无法测试，因此以下所有步骤的「预期结果」都是**待验证的假设**，
> 「实际结果」栏在真机跑完之前**一律留空**。**本文件绝不声称任何步骤已通过。**

测试页：`index.html`（双击用浏览器打开，或 `python3 -m http.server` 后访问；需 Secure Context，
`file://` 在 Chrome 中一般可用，若 query 无反应请改用 `http://localhost`）。

每一步的记录格式：`实际结果` + `截图/备注`。跑完后把结论填进文末的「结论汇总表」。

---

## Step 0 — 准备（每台机器跑一次）

- [ ] 打开 `index.html`，确认第 1 节显示了正确的 UA 与浏览器名称
- [ ] 点击「查询授权状态」，记录初始 permission 状态（预期：`prompt`，**未验证**）

| 记录位 | 填写 |
|---|---|
| 操作系统 / 版本 | |
| 浏览器 / 版本 | |
| 初始 `local-fonts` permission | |
| `window.queryLocalFonts` 是否存在 | |
| `navigator.fonts.query` 是否存在 | |

---

## Step 1 — 首次 prompt 点「允许」

1. 点击「请求本地字体访问权限并列出字体」
2. 在浏览器弹出的授权 prompt 中点**允许**

**预期结果（未验证）：** permission 变为 `granted`；第 3 节列出系统字体，数量 > 0；
疑似 CJK 计数 > 0（中文系统）。

| 记录位 | 填写 |
|---|---|
| 实际结果（是否弹出 prompt / 点允许后状态） | |
| 返回字体总数 | |
| 疑似 CJK 字体列表（复制 family 名） | |
| 截图 | |

---

## Step 2 — 点「拒绝」

1. 在浏览器设置中清除该站点的 `local-fonts` 授权（或换用新的 profile / 隐私窗口重开页面）
2. 点击「请求本地字体访问权限并列出字体」，在 prompt 中点**拒绝**（或「不允许」）

**预期结果（未验证）：** permission 变为 `denied`；query 抛出 `SecurityError`；
运行日志出现 `SecurityError` 字样；第 3 节保持空表。

| 记录位 | 填写 |
|---|---|
| 实际结果 | |
| 日志中的错误名（是否为 SecurityError） | |
| 截图 | |

---

## Step 3 — 关闭 prompt（点 X / 按 ESC / 点击外部）

1. 重置授权状态（同 Step 2 的清除方式）
2. 点击「请求本地字体访问权限并列出字体」，**不点允许也不点拒绝**，直接关闭 prompt

**预期结果（未验证）：** permission 保持 `prompt`（Chrome 行为）；再次点击按钮会重新弹出 prompt；
query 的 Promise 处于 rejected 或 pending 由浏览器实现决定，以日志为准。

| 记录位 | 填写 |
|---|---|
| 实际结果（permission 状态 / 日志内容） | |
| 再次点击是否重新弹出 prompt | |
| 截图 | |

---

## Step 4 — 系统缺 CJK 字体时

在**没有安装 CJK 语言包**的系统上跑（例如英文版 Windows 未装中文语言包、
最小化安装的 Linux 容器桌面）。若手头没有，可在有 CJK 的机器上用浏览器字体设置
临时禁用 CJK 字体近似验证，但必须在记录中注明是近似手段。

**预期结果（未验证）：** 第 3 节疑似 CJK 计数为 0；第 4 节中文段落出现 tofu（□）
或被替换为系统 fallback 字体渲染；第 5 节探测中 CJK 字体全部显示「缺失」。

| 记录位 | 填写 |
|---|---|
| 实际结果（中文是否正常显示 / tofu / fallback） | |
| 第 5 节探测结果（存在 / 缺失列表） | |
| 截图（样本区中文段落） | |

---

## Step 5 — 导出中途切换系统字体

1. 先完成 Step 1（允许授权，字体列表已加载）
2. 在**不刷新页面**的情况下：安装一个新字体（或卸载一个已列出的字体），
   或修改系统默认字体设置
3. 再次点击「请求本地字体访问权限并列出字体」和「探测常见字体是否存在」

**预期结果（未验证）：** 重新 query 后字体列表包含新安装的字体（或不再包含已卸载的）；
第 4 节「用指定字体渲染样本」下拉框同步更新；Phase D provider 若缓存了字体列表，
需要有失效策略（此处只记录现象，不做结论）。

| 记录位 | 填写 |
|---|---|
| 安装/卸载的字体名 | |
| 重新 query 后列表是否变化 | |
| 样本下拉框是否同步更新 | |
| 截图（变化前后） | |

---

## Step 6 — Chrome / Edge / Safari 差异（如有条件）

在每种可用浏览器上重复 Step 0–1，填写下表。

**预期差异（未验证）：**
- Chrome / Edge（Chromium）：应支持 `window.queryLocalFonts`，行为按 Step 1–3
- Safari：`window.queryLocalFonts` 很可能**不存在**，页面应显示「无可用 API」而不是报错白屏
- Firefox：同 Safari，大概率不支持

| 浏览器 | `window.queryLocalFonts` | `navigator.fonts.query` | Step 1 实际结果 | 备注 |
|---|---|---|---|---|
| Chrome | | | | |
| Edge | | | | |
| Safari | | | | |
| Firefox | | | | |

---

## 结论汇总表（跑完后填写，跑完前全部视为未验证）

| 步骤 | 结论（通过 / 失败 / 受阻） | 备注 |
|---|---|---|
| Step 1 允许 | 未验证 | |
| Step 2 拒绝 | 未验证 | |
| Step 3 关闭 prompt | 未验证 | |
| Step 4 缺 CJK 字体 | 未验证 | |
| Step 5 中途切换字体 | 未验证 | |
| Step 6 跨浏览器差异 | 未验证 | |

**回填要求**：结论填好后，把本文件顶部的「一律视为未验证」横幅改为
「已于 YYYY-MM-DD 在 <OS + 浏览器> 上验证」，并保留各步骤的实际结果记录。
